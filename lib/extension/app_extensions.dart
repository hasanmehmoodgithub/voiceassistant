// String str = "There are 25 dogs and 30 cats.";
// final regex = RegExp(r'\d+');
// Iterable<Match> matches = regex.allMatches(str);
// for (Match match in matches) {
// String matchString = match.group(0)!;
// print(matchString);  // prints 25, then 30
// }